{% extends 'base.html.twig' %}

{% block title %}Question: {{ question }}{% endblock %}

{% block body %}
    <h1>{{ question }}</h1>

    {# (*＾▽＾)／(*＾▽＾)／(*＾▽＾)／ #}

    <div>
        Kson is so angry, we don't have the question print here!
    </div>

    <h2>Answers ({{ answers|length }})</h2>

    <ul>
        {% for answer in answers %}
            <li>{{ answer }}</li>
        {% endfor %}
    </ul>
{% endblock %}